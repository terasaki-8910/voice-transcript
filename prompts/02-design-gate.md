# Stage 2 -- Design gate (UI only).

Read design_brief.md (and @~/.claude/rules/ui.md if present). Produce the MINIMUM
for me to approve the aesthetic direction ONCE:
- design tokens (colors, spacing, type scale) as a real file (CSS variables or equiv).
- ONE representative screen using only those tokens.

Constraints: no emoji, no "obviously AI-generated" filler, tokens only (no hardcoded
colors), high usability. This is a direction check, not the full app. After I approve,
these tokens become a machine-enforced constraint for all later work.
